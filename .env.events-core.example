# Connections URL for events-core Postgres DB.
# Used by `events-core-db-setup` CLI to create the database and run migrations.
EVENTS_CORE_DB_URL=postgres://user:password@localhost:5432/events_core_db
